# Ресурсы одной проверки (п. 1.5 состава материалов)

## Прогон для сдачи — измерено

GitHub Actions, run [36037967344](https://github.com/zxaty/KMG-Digital-Hackathon-otcysquad/actions/runs/36037967344) (событие `push`, раннер `ubuntu-24.04`, Python 3.12), коммит `0fba3c18b169d0b0c203fb69f29475aec6affa91`, режим `rules+llm`, модель `deepseek/deepseek-v4-pro`. Отчёт и журнал шага — в [`agent/reports/run-36037967344/`](../reports/run-36037967344/).

| Показатель | Значение |
|---|---|
| Длительность проверки (`meta.duration_seconds`) | **178 с** (≈ 3 мин; лимит шага — 30 мин, внутренний дедлайн — 25 мин) |
| Вызовов модели | **7** (ИБ-06 — без модели), повторов 0, вторых раундов (`need_files`) 0; пригодных ответов 6 из 7 — по ИБ-04 пустой ответ вместо JSON (токены этого вызова входят в итог ниже) |
| Входящие токены (`prompt_tokens`) | **106 875** |
| Исходящие токены (`completion_tokens`) | **37 678** |
| **Итого токенов** | **144 553** |
| Суммарное время ожидания ответов модели (`wall_seconds`, запросы шли по 3 параллельно) | 465,9 с |
| Файлов проекта проиндексировано | 456 |

Время по требованиям (от запроса до ответа, по журналу шага): ИБ-01 — 39 с, ИБ-03 — 48 с, ИБ-08 — 54 с, ИБ-02 — 73 с, ИБ-07 — 74 с, ИБ-05 — 75 с, ИБ-04 — 102 с (самый долгий вызов, завершился непригодным ответом).

Результат: код завершения **1**, **23 нарушения** (критических 4, высоких 15, средних 4), нарушены ИБ-01, 02, 03, 04, 05, 07, 08; ИБ-06 — соответствует; 15 замечаний по ТС.

## Режим «только правила» (`--provider none`) — измерено

Тестовый проект AlmasNUR/KMG-Digital-Hackathon, коммит `576e58100d7ef832cef2ad3ec73fc89ab98f84b5` (453 файла): полный прогон с отчётом — **2–3 с** (индексирование ≈ 0,8 с, правила ≈ 1,2 с), 0 токенов; код 1, 18 нарушений. Образец — [`agent/reports/sample-rules-only/`](../reports/sample-rules-only/).

## Сравнение с предварительной оценкой

До первого прогона с моделью оценка составляла ≈ 104 тыс. входящих токенов (≈ 333 тыс. символов запросов / 3,2), ≈ 7–20 тыс. исходящих, итого ≈ 110–165 тыс., время ≈ 2–6 минут. Фактически: входящие 106 875 (в пределах оценки), исходящие 37 678 (выше оценки: модель подробно обосновывает каждое нарушение), итого 144 553 — внутри диапазона; время 178 с.

Замер сделан на одном прогоне с моделью; разброс между прогонами не измерялся (ответы модели не полностью детерминированы даже при `temperature = 0`, поэтому число исходящих токенов и состав находок модели могут отличаться).
