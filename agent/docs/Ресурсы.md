# Ресурсы одной проверки (п. 1.5 состава материалов)

## Итоговый прогон для сдачи — измерено

GitHub Actions, run [36042637087](https://github.com/zxaty/KMG-Digital-Hackathon-otcysquad/actions/runs/36042637087) (событие `push`, раннер `ubuntu-24.04`, Python 3.12), коммит `4459ed1ca364f208c4bddc5e52a639b6b56dbf09`, режим `rules+llm`, модель `deepseek/deepseek-v4-pro`, режим рассуждений выключен. Отчёт и журнал шага — в [`agent/reports/run-36042637087/`](../reports/run-36042637087/).

| Показатель | Значение |
|---|---|
| Длительность проверки (`meta.duration_seconds`) | **32 с** (лимит шага — 30 мин, внутренний дедлайн — 25 мин) |
| Вызовов модели | **7** (ИБ-06 — без модели); все с `finish_reason=stop`, повторов 0, вторых раундов (`need_files`) 0 |
| Входящие токены (`prompt_tokens`) | **106 322** |
| Исходящие токены (`completion_tokens`) | **6 628** |
| **Итого токенов** | **112 950** |
| Суммарное время ожидания ответов (`wall_seconds`, запросы по 3 параллельно) | 68,7 с |
| Файлов проекта проиндексировано | 456 |
| Стоимость (по прайсу DeepSeek вне часов пик: $0,66 / 1 млн входящих, $0,60 / 1 млн исходящих) | ≈ $0,07 |

Время по требованиям (от запроса до ответа, по журналу шага): ИБ-08 — 5 с, ИБ-04 — 6 с, ИБ-05 — 8 с, ИБ-01 — 9 с, ИБ-02 — 9 с, ИБ-03 — 13 с, ИБ-07 — 18 с. Расход по каждому вызову (`finish_reason`, токены, длина ответа) — в `resources.llm.per_call` отчёта.

Результат: код завершения **1**, **25 нарушений** (критических 3, высоких 19, средних 3), нарушены ИБ-01, 02, 03, 04, 05, 07, 08; ИБ-06 — соответствует; 16 замечаний по ТС; раздел «Ограничения» пуст.

## Предыдущий прогон (для сравнения)

Run [36037967344](https://github.com/zxaty/KMG-Digital-Hackathon-otcysquad/actions/runs/36037967344), коммит `0fba3c1`, режим рассуждений DeepSeek включён по умолчанию: 178 с, 7 вызовов, 106 875 входящих + 37 678 исходящих = 144 553 токена (≈ $0,09), 23 нарушения; по ИБ-04 ответ модели был пустым. Отчёт — [`agent/reports/run-36037967344/`](../reports/run-36037967344/). Выключение режима рассуждений сократило исходящие токены в 5,7 раза и время в 5,6 раза при почти том же объёме входящих.

## Режим «только правила» (`--provider none`) — измерено

Тестовый проект AlmasNUR/KMG-Digital-Hackathon, коммит `576e58100d7ef832cef2ad3ec73fc89ab98f84b5` (453 файла): полный прогон с отчётом — **2–3 с** (индексирование ≈ 0,8 с, правила ≈ 1,2 с), 0 токенов; код 1, 18 нарушений. Образец — [`agent/reports/sample-rules-only/`](../reports/sample-rules-only/).

## Сравнение с предварительной оценкой

До первого прогона с моделью оценка составляла ≈ 104 тыс. входящих токенов, ≈ 7–20 тыс. исходящих, итого ≈ 110–165 тыс., время ≈ 2–6 минут. Итоговый прогон: 106 322 входящих (в пределах оценки), 6 628 исходящих (ниже оценки), 112 950 итого, 32 с.

Замеры сделаны на двух прогонах с моделью; разброс между прогонами при одинаковых настройках систематически не измерялся.
