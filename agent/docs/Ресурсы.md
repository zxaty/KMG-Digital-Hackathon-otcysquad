# Ресурсы одной проверки (п. 1.5 состава материалов)

## Итоговый прогон для сдачи — измерено

GitHub Actions, run [36096594643](https://github.com/zxaty/KMG-Digital-Hackathon-otcysquad/actions/runs/36096594643) (событие `push`, раннер `ubuntu-24.04`, Python 3.12), коммит `de7017c3188b665480961742738a2498bb830f7a`, режим `rules+llm`, модель `deepseek/deepseek-v4-pro`, режим рассуждений выключен. Отчёт, журнал шага и скриншот — в [`agent/reports/run-36096594643/`](../reports/run-36096594643/).

| Показатель | Значение |
|---|---|
| Длительность проверки (`meta.duration_seconds`) | **24 с** (лимит шага — 30 мин, внутренний дедлайн — 25 мин) |
| Вызовов модели | **7** (ИБ-06 — без модели); все с `finish_reason=stop`, повторов 0, вторых раундов (`need_files`) 0 |
| Входящие токены (`prompt_tokens`) | **106 322** |
| Исходящие токены (`completion_tokens`) | **5 789** |
| **Итого токенов** | **112 111** |
| Суммарное время ожидания ответов (`wall_seconds`, запросы по 3 параллельно) | 50,6 с |
| Файлов проекта проиндексировано | 456 |
| Стоимость (по прайсу DeepSeek вне часов пик: $0,66 / 1 млн входящих, $0,60 / 1 млн исходящих; прогон начат в 04:58 UTC) | ≈ $0,07 |

Время по требованиям (от запроса до ответа, по журналу шага): ИБ-08 — 4 с, ИБ-03 — 5 с, ИБ-04 — 5 с, ИБ-05 — 6 с, ИБ-02 — 8 с, ИБ-01 — 9 с, ИБ-07 — 15 с. Расход по каждому вызову (`finish_reason`, токены, длина ответа) — в `resources.llm.per_call` отчёта.

Результат: код завершения **1**, **19 нарушений** (критических 3, высоких 14, средних 2), нарушены ИБ-01, 02, 03, 04, 05, 07, 08; ИБ-06 — соответствует; 15 замечаний по ТС; раздел «Ограничения» пуст.

## Предыдущие прогоны (история исправлений, для сравнения)

| Прогон | Коммит | Время | Токены (вход + выход = итого) | Нарушений | Что было не так |
|---|---|---|---|---|---|
| [36037967344](https://github.com/zxaty/KMG-Digital-Hackathon-otcysquad/actions/runs/36037967344) | `0fba3c1` | 178 с | 106 875 + 37 678 = 144 553 (≈ $0,09) | 23 | режим рассуждений включён; по ИБ-04 пустой ответ модели |
| [36042637087](https://github.com/zxaty/KMG-Digital-Hackathon-otcysquad/actions/runs/36042637087) | `4459ed1` | 32 с | 106 322 + 6 628 = 112 950 (≈ $0,07) | 25 | одна проблема ИБ-07 посчитана 6 раз; два фрагмента кода искажены фильтром секретов |

Выключение режима рассуждений сократило исходящие токены примерно в 6 раз и время проверки в 5–7 раз при том же объёме входящих. Отчёты — в [`agent/reports/`](../reports/).

## Режим «только правила» (`--provider none`) — измерено

Тестовый проект AlmasNUR/KMG-Digital-Hackathon, коммит `576e58100d7ef832cef2ad3ec73fc89ab98f84b5` (453 файла): полный прогон с отчётом — **2–3 с** (индексирование ≈ 0,8 с, правила ≈ 1,2 с), 0 токенов; код 1, 18 нарушений. Образец — [`agent/reports/sample-rules-only/`](../reports/sample-rules-only/).

## Сравнение с предварительной оценкой

До первого прогона с моделью оценка составляла ≈ 104 тыс. входящих токенов, ≈ 7–20 тыс. исходящих, итого ≈ 110–165 тыс., время ≈ 2–6 минут. Итоговый прогон: 106 322 входящих (в пределах оценки), 5 789 исходящих (ниже оценки), 112 111 итого, 24 с.

Замеры сделаны на трёх прогонах с моделью; разброс между прогонами при одинаковых настройках систематически не измерялся.
